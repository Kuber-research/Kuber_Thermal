# Pinned to the exact versions used to train and evaluate the models in this repo.
# (Captured from the training environment.)

torch==2.12.1
numpy==2.4.6

# GeoTransolver core lives in NVIDIA PhysicsNeMo (experimental module):
#   physicsnemo.experimental.models.geotransolver.GeoTransolver
# Install per https://github.com/NVIDIA/physicsnemo (it may need a CUDA/torch-matched build).
nvidia-physicsnemo==2.1.1

# torch above is the CPU/build-agnostic pin; for a specific CUDA build install torch from the
# matching PyTorch index, e.g.:  pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu130
#
# Data generation (datagen/) additionally requires an OpenFOAM install on PATH
# (buoyantSimpleFoam, blockMesh, snappyHexMesh) - not a pip dependency.
# The Colab quickstart (notebooks/) installs matplotlib itself; it is not needed for training/eval.
